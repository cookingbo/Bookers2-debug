<div>
  <% if book.user != current_user %>
    <%= form_with model: [book, book_comment_new], data: {remote: true} do |f| %>
      <%= f.text_area :comment, rows: '5', class: "form-control" %><br>
      <%= f.submit "送信" %>
    <% end %>
  <% end %>
</div>